// Auth Interfaces
export interface loginFormData{
    email: string;
    password: string;
}

// Categories Interfaces
export interface categoriesFormData{
    name: string|null;
    imageUrl: string;
    id: string
}